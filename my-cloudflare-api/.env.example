# Cloudflare Workers Environment Variables Example
# Copy this file to .env and fill in your actual values

# Database
DB_ID=your_d1_database_id

# Email Service (Resend)
RESEND_API_KEY=your_resend_api_key
SMTP_FROM_EMAIL=your_verified_email@yourdomain.com

# SMTP Configuration (optional, for backup email service)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Application URL
APP_URL=https://your-worker-name.your-subdomain.workers.dev

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=https://your-worker-name.your-subdomain.workers.dev/auth/google/callback

# Note: These values should be set in Cloudflare Workers dashboard under Settings > Variables
# or using wrangler secret commands for sensitive data
